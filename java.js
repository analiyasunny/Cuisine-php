function confrimDelete(){
    if(confirm('Are you sure you want to delete this?')){
      //ok to excute the link
    return true;
  }
    else{
      //cancel to stop execution
      return false;
    }
  }